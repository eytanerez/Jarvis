import JarvisCore

// ContextPack is defined in JarvisCore because BrainChatRequest also needs the
// same Codable shape. This file keeps the JarvisContext source layout aligned
// with the context-engine boundary.
